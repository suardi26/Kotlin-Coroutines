/**
    joinAll Function
    - Kadang kita akan membuat coroutine lebih dari satu sekaligus.
    - Untuk menunggu semua Job coroutine selesai berjalan kita dapat menggunakan join() function
    - Namun jika kita panggil satu-satu Job coroutine-nya, akan sangat mengganggu sekali.
    - Kotlin menyediakan joinAll(jobs) function untuk menunggu semua job selesai.
 */
package joinall.function

class App {
    // Contohnya pada Test.
}


