/**
    Membuat Coroutine
    - Coroutine tidak dapat berjalan sendiri, dia perlu berjalan di dalam sebuah Scope(Coroutine Scope).
    - Salah satu scope(Coroutine Scope) yang dapat kita gunakan adalah GlobalScope (masih banyak scope yang ada).
    - Untuk membuat Coroutine, kita dapat menggunakan method launch().
    - Dan di dalam coroutine, kita dapat memanggil suspend function.

    Coroutine sangat ringan
    - Coroutine itu ringan dan cepat, sehingga saat kita membuat coroutine dalam jumlah besar, ini tidak akan berdampak
      terlalu besar dengan memory yang kita gunakan.
    - Sekarang kita akan coba bandingkan membuat thread dan coroutine dalam jumlah banyak.

    

 */
package create.coroutine

class App {
    // Contohnya ada di bagian Test.
}


